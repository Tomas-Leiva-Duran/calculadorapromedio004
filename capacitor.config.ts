import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.calculadorapromedio004',
  appName: 'calculadorapromedio004',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
